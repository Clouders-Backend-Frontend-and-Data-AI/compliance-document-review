# Base URL of the FastAPI backend's v1 API
VITE_API_BASE_URL=http://localhost:8000/api/v1

# Set to true to run the whole app against in-memory mock data — no backend needed.
# Demo logins: advisor@example.com / Advisor123!  and  officer@example.com / Officer123!
VITE_USE_MOCK=false
